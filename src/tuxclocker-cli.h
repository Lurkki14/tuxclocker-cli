#ifndef TUXCLOCKER_CLI_H_
#define TUXCLOCKER_CLI_H_

const uint8_t MAX_GPUS = 64; // Used for some functions that require a size as an argument

// Print the GPU name and the index the program uses
void print_gpu_info();

#endif
